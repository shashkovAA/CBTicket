datasource.mssql.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
datasource.mssql.url=jdbc:sqlserver://127.0.0.1:1433;database=SBRCallback
datasource.mssql.username=sa
datasource.mssql.password=Avaya123*

spring.jpa.hibernate.ddl-auto=update

spring.jpa.database-platform=org.hibernate.dialect.SQLServer2012Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.thymeleaf.cache=false

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/temp/cbticket/uploads
file.download-dir=/temp/cbticket/downloads

#Ticket section
#ticket.maxAttempts=3